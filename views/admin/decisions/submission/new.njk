{% extends "admin/base.njk" %}

{% from "govuk/components/button/macro.njk" import govukButton %}

{% set title = ("decisions.admin.submission.caption" | t) %}

{% block content %}

  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
      <span class="govuk-caption-l">{{ title }}</span>
      <h1 class="govuk-heading-l">{{ ('decisions.admin.submission.heading' | t )}}</h1>
      {% include "../_dataset-details.njk" %}
      <form action="/admin/decisions/{{profession.id}}/{{organisation.id}}/{{year}}/submit?_method=PUT" method="post">
        {{
          govukButton({
            text: "decisions.admin.buttons.submit" | t,
            classes: "govuk-button",
            id: 'submit-button'
          })
        }}
      </form>
    </div>
  </div>

{% endblock %}
